{% extends 'Layouts/header.html.twig' %}

{% block body %}
	<div class="container-fluid">
		<h1 class="h3 mb-2 text-gray-800">shopgroups</h1>
		<p class="mb-4">Informacion de shopgroupos</p>


		<div class='card'>
			<div class='card-body'>
				<form id='shopgroups' method="POST" action='{{ (data is null)? path('create_shopgroup'): path('update_shopgroup', {id: data.id}) }}'>
					<input type="hidden" name="token" value="{{ csrf_token('delete-item') }}"/>

					<div class='row'>
						<div class='col-sm-12'>
							<div class='form-group'>
								<div class='form-group'>
									<label for='email'>Nombre</label>
									<input type='text' class='form-control' name='name' value='{{ (data is null)? '': data.name }}' required/>
								</div>
							</div>
						</div>
					</div>

					<input type="submit" class='btn btn-primary' value='{{ (data is null)? 'Crear': 'Actualizar' }}'/>


				</form>
			</div>
		</div>
	</div>
{% endblock %}
